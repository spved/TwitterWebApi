defmodule TwitterPheonixInController do
  @moduledoc """
  TwitterPheonix keeps the contexts that define your domain
  and business logic.

  Contexts are also responsible for managing your data, regardless
  if it comes from the database, an external API or others.
  """
  #TwitterPheonixWeb.Twitter.twitterStart(100,10)
  IO.inspect "Instead starting here right"

  IO.inspect "Testing Engine here in new start"

end
